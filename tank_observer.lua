--- This script is used to see how much a tank is full or empty or other info too.
--- Works with barrels, tanks, BAT9000.

local component = require("component")
local shell = require("shell")

local tank = component.ntm_fluid_tank

shell.execute("component")

if


